# GenericAgent 容器内 Python 依赖
# 说明: 核心依赖(agent 引擎 + TMWebDriver 本地服务) + UI + 全部 IM Bot SDK
# 刻意不装: pywebview(需桌面 GUI) / PySide6(qtapp 桌面界面, 容器无显示, 面板已禁用)
# 本地可选模块: memory/L4_raw_sessions/compress_session.py (scheduler 内 try/except 降级)

# --- 核心 ---
requests>=2.28
beautifulsoup4>=4.12
bottle>=0.12
simple-websocket-server>=0.4
aiohttp>=3.9

# --- UI ---
streamlit>=1.28
prompt_toolkit>=3.0,<4
rich>=13.0
pillow>=9.0
textual>=0.40

# --- Web 服务 (conductor.py) ---
fastapi>=0.100
uvicorn>=0.23

# --- IM Bot SDK (面板一键启用全部客户端) ---
python-telegram-bot>=20.0     # tgapp.py
discord.py>=2.3               # dcapp.py
qq-botpy>=1.0                 # qqapp.py
lark-oapi>=1.0                # fsapp.py
dingtalk-stream>=0.17         # dingtalkapp.py
wecom-aibot-sdk>=1.0          # wecomapp.py
pycryptodome>=3.19            # wechatapp.py
qrcode>=7.4                   # wechatapp.py

# --- 辅助 ---
psutil>=5.9
Markdown>=3.4
