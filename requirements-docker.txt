# GenericAgent 容器内 Python 依赖
# 说明: 核心依赖(agent 引擎 + TMWebDriver 本地服务) + UI 子集
# 刻意不装: pywebview(需桌面 GUI, 容器无用) / textual(tui_v3 用 prompt_toolkit)
# 若需 IM bot 前端(Telegram/Discord/Lark 等): 追加 python-telegram-bot, lark-oapi 等

# --- 核心 ---
requests>=2.28
beautifulsoup4>=4.12
bottle>=0.12
simple-websocket-server>=0.4
aiohttp>=3.9

# --- UI ---
streamlit>=1.28
prompt_toolkit>=3.0,<4
rich>=13.0
pillow>=9.0
